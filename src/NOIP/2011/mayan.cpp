#include <cstdio>
#include <iostream>

int main() {
    freopen("mayan.in", "r", stdin);
    freopen("mayan.out", "w", stdout);

    std::cout << -1 << '\n';

    fclose(stdin);
    fclose(stdout);
    return 0;
}